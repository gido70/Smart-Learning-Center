# AGENTS.md — Smart Learning Center

## الغرض
هذه القواعد ملزمة لأي أداة ذكاء اصطناعي أو مطور يعمل داخل المشروع.

## قواعد الحماية
1. لا تحذف أي وظيفة أو صفحة أو زر قائم إلا بطلب صريح ومحدد.
2. عدّل أقل عدد ممكن من الملفات لتنفيذ المهمة.
3. لا تغيّر الهوية البصرية أو RTL أو استجابة الهاتف أثناء إصلاح وظيفة.
4. كل ميزة جديدة تُبنى داخل وحدة مستقلة تحت `src/modules/`.
5. لا توضع مفاتيح Supabase أو مفاتيح الذكاء الاصطناعي داخل الواجهة أو GitHub.
6. قبل التسليم شغّل `python tests/smoke_test.py`.
7. إذا فشل اختبار قديم، لا يُعتمد التعديل قبل إصلاحه.
8. اذكر في سجل التغيير كل ملف تم إنشاؤه أو تعديله.
9. احتفظ بالتوافق مع GitHub Pages والمسارات النسبية.
10. لا تنقل أو تعيد تسمية ملف قائم دون توثيق سبب ذلك.

## طريقة العمل
- فرع مستقل لكل ميزة: `feature/<name>` أو `fix/<name>`.
- إصلاحات صغيرة ومحددة.
- مراجعة `git diff` قبل الدمج.
- تحديث `CHANGELOG.md` ورقم الإصدار عند كل تسليم.

## هوية المشروع المعتمدة

- اسم المشروع: Smart Learning Center — مركز التعلّم الذكي.
- مستودع GitHub المعتمد: gido70/Smart-Learning-Center.
- رابط المنصة: [https://gido70.github.io/Smart-Learning-Center/](https://gido70.github.io/Smart-Learning-Center/)
- Supabase Project Ref المعتمد: nmbbahzzogspuuvpsxud.
- يمنع الاتصال أو تنفيذ أي عملية على مشروع Supabase آخر.
- لا يُعاد تنفيذ أي ملف SQL قديم دون فحص سجل الترحيلات وموافقة عبدالرحمن.
- كل تغيير جديد في قاعدة البيانات يجب حفظه مستقبلًا داخل supabase/migrations قبل تنفيذه.

## قواعد الأتمتة والموافقة

- GitHub هو المصدر الأساسي والنسخة المعتمدة للمشروع.
- الإصدار المرجعي الحالي هو 3.6.1.
- كل تعديل مهم يجب أن يتم داخل فرع آمن مستقل.
- يمنع تعديل أو دمج main مباشرة دون مراجعة عبدالرحمن.
- يجب تشغيل الاختبارات قبل طلب الدمج.
- يمنع تنفيذ SQL أو الاتصال بـSupabase أو تعديل قاعدة البيانات دون موافقة صريحة.
- يمنع نشر GitHub Pages دون موافقة عبدالرحمن.
- يمنع وضع كلمات المرور أو service_role أو أي أسرار داخل المستودع.
- مفتاح Supabase العام publishable/anon لا يعامل ككلمة مرور، لكن الحماية تعتمد على RLS.
- لأغراض قاعدة الحماية رقم 5، المقصود بمفاتيح Supabase المحظورة هو service_role والمفاتيح السرية؛ أما publishable/anon العام فيجوز وجوده في الواجهة مع RLS سليم.
- يجب عرض الملفات المتغيرة ونتائج الاختبارات والمخاطر قبل الموافقة.
- التقرير النهائي يكون باللغة العربية وداخل كتلة Markdown واحدة قابلة للنسخ.
- لا تعمل أكثر من أداة ذكاء اصطناعي على الملف نفسه في الوقت نفسه.
